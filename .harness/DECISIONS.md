# DECISIONS — 주요 결정 로그

> 사용자와 합의된 아키텍처/워크플로우 결정을 기록한다. 나중에 같은 논의를 반복하지 않기 위함이다.
> 각 항목에는 날짜, 결정 내용, 이유(왜 다른 대안 대신 이것을 선택했는지)를 남긴다.
> 최신 결정을 위에 둔다. 새 결정은 표의 첫 번째 데이터 행에 추가한다.

| 날짜 | 결정 | 이유 |
|------|------|------|
| 2026-07-06 | DB 테이블·컬럼·제약·인덱스와 기준 데이터의 단일 진실 소스는 Flyway 마이그레이션으로 두고, `ARCHITECTURE.md`에는 상세 스키마를 중복 기재하지 않는다 | 이미 적용된 마이그레이션과 설명 문서를 함께 수정하면서 두 정의가 어긋나는 문제를 막고, 스키마 변경 이력을 실행 가능한 SQL 한 곳에서 관리하기 위해 |
| 2026-07-06 | `payment`·`purchase` 멱등키는 사용자별 복합 유니크로 관리하고, `credit_transaction.payment_id`는 `payment.id`를 참조하는 nullable BIGINT FK, `payment.paid_at`은 DATETIME(6)으로 저장한다 | 멱등키 재사용 범위가 인증 사용자 단위라는 API 정책을 DB 제약과 일치시키고, 관계와 시각 값을 문자열로 저장해 생기는 무결성·정렬 문제를 방지하기 위해 |
| 2026-07-06 | 유효한 요청 `X-Request-Id`는 재사용하고 없거나 유효하지 않으면 UUID를 생성해 응답 헤더·오류 본문·로그 MDC에 동일하게 제공한다 | 클라이언트와 서버 로그 사이에서 한 요청을 일관되게 추적하되 비어 있거나 지나치게 길거나 제어 문자를 포함한 식별자가 로그 문맥을 오염시키지 않도록 하기 위해 |
| 2026-07-06 | `PLAN.md` 단계별 체크리스트 항목은 이슈(또는 모듈) 구현이 끝날 때마다 즉시 `.harness/STATE.md`의 완료된 작업에 반영하고 `PLAN.md`에서 그 항목을 제거한다 | 전체 단계가 끝날 때까지 체크리스트가 그대로면 이슈별로 진행하는 지금 방식에서 어디까지 끝났는지 PLAN.md만 보고 알 수 없음. 완료 표시(`- [x]`)만 남기기보다 STATE.md로 옮겨 PLAN.md를 남은 작업만 보이는 상태로 유지 |
| 2026-07-06 | master에 병합한 feature 브랜치는 즉시 삭제하고 master로 전환한 상태를 유지하며, 다음 이슈는 항상 master에서 새 브랜치를 만든다 | 병합이 끝난 브랜치를 남겨두면 다음 이슈 브랜치를 어디서 분기했는지 헷갈리고 오래된 브랜치가 쌓임. 항상 최신 master에서 분기해야 이슈 간 변경 반영 누락을 방지 |
| 2026-07-06 | 이슈 하나당 브랜치 하나를 원칙으로 하되 커밋 개수는 제한하지 않는다. 여러 이슈를 포함하는 모듈 단위 작업은 한 브랜치에서 진행할 수 있고, 이 경우 모듈 내 가장 낮은 이슈 번호를 대표로 사용한다. 브랜치(또는 모듈) 구현·커밋 후 바로 master에 병합하고, 사용자 확인을 받은 뒤에만 다음 이슈/모듈 브랜치를 만든다. 브랜치/커밋/병합은 로컬에서만 진행하고 원격 push·PR은 사용자 요청 시에만 수행 | 이슈별 이력 추적과 매 단계 사용자 확인은 유지하되, 커밋을 이슈당 하나로 강제하거나 모듈 단위 번들링을 금지하는 것은 과도하다는 사용자 피드백 반영 |
| 2026-07-06 | Claude Code `PreCompact` 훅(`.claude/settings.json`)을 추가해 컨텍스트 압축 직전 `.harness/STATE.md`·`HANDOFF.md` 갱신을 자동으로 상기시킴. `Stop` 훅은 매 턴마다 발화해 노이즈가 커서 제외 | 세션이 갑자기 compact되어 인수인계 없이 맥락이 끊기는 것을 방지. 이 훅은 Claude Code 전용이라 Codex/Cursor에는 적용되지 않음 |
| 2026-07-06 | `DECISIONS.md`는 최신 결정이 위로 오도록 관리하고 새 결정은 표 최상단에 추가 | 세션 시작 시 최근 변경 사항과 판단 근거를 먼저 파악할 수 있도록 하기 위해 |
| 2026-07-06 | `.harness/PLAN.md`에는 제안·확정·진행 중인 계획을 유지하고, 완료된 계획은 결과를 해당 산출물에 반영한 뒤 제거 | 실행 중인 계획의 연속성은 유지하면서 `STATE.md`·`HANDOFF.md` 등에 존재하는 완료 이력의 중복을 방지하기 위해 |
| 2026-07-06 | KRW 기준 초기 크레딧 상품은 BASIC 500/5,000원, PLUS 1,000+100/10,000원, PRO 3,000+500/30,000원으로 Flyway seed 등록하고 활성 상품 조회만 제공 | 목업 결제와 크레딧 흐름을 즉시 검증할 수 있는 고정 상품군을 두고 클라이언트 가격 조작과 운영 중 물리 삭제를 방지하기 위해 |
| 2026-07-06 | 콘텐츠당 태그를 최대 5개로 제한하고 1~30자 이름의 공백·영문 대소문자를 정규화해 기존 태그를 재사용. 미사용 태그는 보존하고 태그 API는 검색·사용량순 최대 50개를 제공 | 중복·표기 차이로 태그가 파편화되는 것을 막고 콘텐츠 등록 편의와 탐색 자동완성을 지원하기 위해 |
| 2026-07-06 | 회원가입은 정규화된 고유 이메일, 고유 닉네임, BCrypt 비밀번호를 사용하고 기본값은 `USER`·`ACTIVE`·0 크레딧. 이메일 인증과 탈퇴 API는 MVP에서 제외하고 로그인 실패 원인은 통합 | MVP 가입 흐름을 단순하게 유지하면서 중복 계정·닉네임을 방지하고 비밀번호 원문 및 이메일 존재 여부 노출을 막기 위해 |
| 2026-07-06 | 모든 Asset에 10MB 이하 이미지 커버를 요구하고 원본은 IMAGE 50MB, VIDEO 1GB, MUSIC 200MB로 제한. Presigned URL 전후로 형식·크기를 검증하고 미등록 임시 객체는 24시간 후 정리 | 미디어 변환 없이 일관된 공개 미리보기를 제공하고 직접 업로드의 위조·고아 객체를 제한하면서 MVP에서 처리 가능한 파일 범위를 명확히 하기 위해 |
| 2026-07-06 | 콘텐츠 판매 수수료는 20%를 버림 계산하고 창작자에게 나머지 크레딧을 구매 트랜잭션에서 즉시 지급. 정산은 즉시 `SETTLED`로 기록하고 구매자·창작자 행은 ID 순서로 잠금 | 별도 현금 출금 없이 내부 크레딧 경제를 완결하고 잔액·거래·정산의 원자성을 보장하면서 동시 구매의 교착 상태 위험을 줄이기 위해 |
| 2026-07-06 | Category를 별도 테이블로 관리하고 Asset은 `category_id` FK로 참조. `category.name`은 Java enum을 `EnumType.STRING`으로 저장하며 `NATURE`, `PEOPLE`, `BUSINESS`, `TECHNOLOGY`, `FANTASY`, `ABSTRACT`, `LIFESTYLE`, `OTHER`를 Flyway seed로 등록하고 비활성화로 제거 | 프론트엔드의 `categoryId` 계약과 DB 모델을 일치시키고 잘못된 카테고리 참조를 방지하며, enum 순서 변경과 기존 콘텐츠의 카테고리 보존에 안전하도록 하기 위해 |
| 2026-07-06 | `GET /api/contents`의 정렬은 `Querydsl` 동적 쿼리로 조합하고 `LATEST`·`POPULAR`·`PRICE_ASC`·`PRICE_DESC`를 지원. 검색은 MySQL `LIKE` 기반으로 제목·설명·태그를 대상으로 하며 `type`, `categoryId`, `tag`, `minPrice`, `maxPrice`, `creatorId` 필터를 제공 | 정렬/필터 조합이 많아 고정 쿼리보다 동적 조건 조합이 유지보수에 유리하고, MVP에서는 별도 검색 엔진 없이도 충분한 탐색 경험을 제공할 수 있기 때문에 |
| 2026-07-06 | `GET /api/dashboard/sales`는 `period`(`7D`·`30D`·`ALL`)를 받고 `totals`·`series`·`items`를 함께 반환하며 `items`는 판매 상위 5개를 최신순 타이브레이크로 제공 | 창작자가 현재 성과와 기간별 추이를 한 번에 확인할 수 있어야 하고, 프론트가 대시보드 한 번 호출로 핵심 지표를 렌더링할 수 있기 때문에 |
| 2026-07-06 | `asset`에 `view_count`만 추가하고 `like_count`는 추가하지 않음 | 프론트가 조회수로 정렬(`sort:-views`)하고 표시하므로 실제 컬럼이 필요. 좋아요는 표시 필드만 있고 "누르는" 상호작용이 프론트 어디에도 없어 지금 스키마에 넣을 근거가 없음 — 필요해지면 그때 추가 |
| 2026-07-06 | `GET /api/contents/{id}` 호출마다 `view_count`를 무조건 1 증가 (중복 방지 없음) | 세션/캐시 기반 중복 방지는 구현 복잡도를 늘리고 MVP 범위를 벗어남. 정확한 유니크 조회수보다 단순 카운터로 충분 |
| 2026-07-06 | `GET /api/dashboard/sales`는 `totals`(총계)와 `items`(콘텐츠별 판매 목록)를 함께 반환 | Dashboard.jsx가 총 등록수/총 판매횟수/누적 크레딧과 콘텐츠별 판매횟수를 동시에 표시하므로 한 번의 호출로 두 정보를 다 받는 것이 프론트 구현을 단순하게 함. `sales`는 `asset`에 비정규화하지 않고 `purchase`/`creator_settlement`에서 매번 집계 |
| 2026-07-06 | `GET /api/contents` 쿼리 파라미터에 `creatorId`를 공식 추가 | Dashboard.jsx가 창작자 본인 콘텐츠 조회에 이 필터를 이미 사용 중이라 명세에 명시하지 않으면 계약이 암묵적으로 남음 |
| 2026-07-05 | Flyway SQL만 DB 스키마를 변경하고 JPA는 모든 환경에서 `ddl-auto=validate`를 사용. 로컬은 Docker Compose MySQL·MinIO, DB 통합 테스트는 Flyway가 적용된 Testcontainers MySQL을 사용 | 환경마다 동일한 스키마 변경 이력을 재현하고 MySQL의 실제 제약·쿼리·잠금 동작을 기준으로 검증하기 위해 |
| 2026-07-05 | Asset은 `PUBLISHED`·`DELETED` 상태로 소프트 삭제하고 기존 구매자의 조회·다운로드 권리는 유지. 구매 가격·라이선스는 구매 이력에 보존하며 최초 판매 후 원본과 라이선스 변경을 금지 | 판매자가 콘텐츠를 삭제·변경해도 이미 구매한 사용자의 권리와 구매 당시 조건을 보존하고 거래 이력의 일관성을 유지하기 위해 |
| 2026-07-05 | API는 단건 `{data}`, 목록 `{data, page}`, 오류 `{code, message, fieldErrors, requestId}` 형식을 사용하고 페이지는 0부터 시작해 기본 20·최대 100으로 제한. HTTP 상태 코드를 의미에 맞게 사용 | Spring 내부 객체 노출을 피하고 프론트엔드가 성공·페이지·오류를 일관되게 처리하며 과도한 목록 요청을 제한하기 위해 |
| 2026-07-05 | 크레딧 목업 결제는 활성 상품이면 즉시 성공하고 서버의 상품 정보로 금액과 지급 크레딧을 결정. 결제·잔액 증가·거래 이력을 단일 트랜잭션으로 처리하고 `Idempotency-Key` 재요청에는 최초 결과를 반환 | 외부 결제 연동 없이 핵심 크레딧 흐름을 검증하면서 클라이언트의 금액 조작과 네트워크 재시도의 중복 충전을 방지하기 위해 |
| 2026-07-05 | 콘텐츠 구매 시 사용자 잔액 행을 비관적 잠금하고 잔액 차감·거래·구매·정산 생성을 단일 트랜잭션으로 처리. 사용자와 콘텐츠 조합 및 `Idempotency-Key`를 유일하게 관리하고 본인 콘텐츠 구매는 금지 | 동시 요청의 잔액 초과 차감과 네트워크 재시도의 중복 구매를 방지하고, 구매 처리 일부만 반영되는 데이터 불일치를 막기 위해 |
| 2026-07-05 | Asset 파일 위치는 URL 대신 `preview_object_key`와 `original_object_key`로 저장하고 원본 파일명·MIME 타입·파일 크기를 함께 보관 | Presigned URL 만료와 저장소 도메인 변경의 영향을 DB에서 제거하고, MinIO/S3 전환 및 업로드 검증에 필요한 파일 메타데이터를 유지하기 위해 |
| 2026-07-05 | 운영 파일 저장소는 AWS S3, 로컬 개발은 MinIO를 사용하고, 원본은 비공개·미리보기는 공개로 저장. 업로드와 구매 후 다운로드는 Presigned URL을 사용하며 미디어 변환은 MVP에서 제외 | 영상·음악을 백엔드 서버가 중계하는 부하를 피하고 개발·운영 환경에서 동일한 S3 API를 사용하면서 원본 접근을 구매 권한으로 제한하기 위해 |
| 2026-07-05 | Refresh token은 로그인 기기별로 발급하고 원문 대신 해시를 MySQL `refresh_token` 테이블에 저장하며, 재발급 시 회전하고 로그아웃 시 해당 세션만 폐기 | 여러 기기의 로그인을 독립적으로 유지하면서 토큰 탈취 시 DB 원문 노출 위험을 줄이고, 현재 기술 스택에 없는 Redis 도입을 피하기 위해 |
| 2026-07-05 | Access token은 프론트엔드 메모리에 15분간 유지하고, Refresh token은 `HttpOnly`·`Secure` 쿠키로 14일간 유지하며 재발급 시 회전. 로그아웃 시 Refresh token만 폐기하고 Access token 블랙리스트는 사용하지 않음 | 브라우저 스크립트의 Refresh token 접근을 차단하고, 짧은 Access token 수명으로 블랙리스트 저장소와 매 요청 조회 없이 로그아웃 후 위험 시간을 제한하기 위해 |
| 2026-07-05 | 모든 일반 사용자(`USER`)가 콘텐츠를 구매하고 판매할 수 있으며, 창작자는 별도 역할이 아니라 콘텐츠 소유 관계로 판단 | `BUYER`와 `CREATOR`를 분리할 때 필요한 역할 전환·복수 역할 정책을 피하고 MVP 권한 모델을 단순화하기 위해 |
| 2026-07-05 | Claude Code/Codex/Cursor 세 도구가 공유하는 연속성 하네스를 파일 기반(`.harness/`)으로 구성 | harness 메타 스킬의 에이전트 팀 기능(`TeamCreate`/`SendMessage`)은 Claude Code 전용이라 Codex/Cursor가 읽을 수 없음. 도구 중립적인 마크다운 산출물이 세 도구 모두에서 동작하는 유일한 공통 분모 |
| 2026-07-05 | 새 기능은 반드시 `PLAN.md`에 계획을 먼저 쓰고 사용자 컨펌 후 구현 | 세션/도구가 바뀌어도 "무엇을 왜 하기로 했는지"가 유실되지 않도록 |
| 2026-07-05 | `/SPEC.md`, `/frontend/SPEC.md`, `/backend/SPEC.md`를 `.harness/ARCHITECTURE.md`로 통합하고 원본 삭제 | 명세 문서가 여러 곳에 흩어져 있으면 하네스의 "단일 진실 소스" 원칙과 충돌. 프론트/백엔드 README는 유지하되 상세 명세 링크만 통합 문서로 변경 |
